import React from 'react';
import useTitle from '../../Hooks/TitleHooks';

const AddToys = () => {
    useTitle('Add Toys')
    return (
        <div>
            add toys
        </div>
    );
};

export default AddToys;